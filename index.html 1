<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>For My Alexis</title>
    <link href="https://fonts.googleapis.com/css2?family=Great+Vibes&family=Poppins:wght@300;400;600&display=swap" rel="stylesheet">
    <style>
        :root {
            --bg-color: #a1c4fd;
            --container-bg: rgba(240, 248, 255, 0.95);
            --accent-color: #1a73e8;
            --accent-hover: #1254a8;
            --text-color: #4a4a4a;
            --shadow-sm: 0 4px 15px rgba(26, 115, 232, 0.1);
            --shadow-md: 0 15px 35px rgba(26, 115, 232, 0.2);
            --gradient-bg: linear-gradient(135deg, #a1c4fd 0%, #c2e9fb 100%);
        }

        body {
            margin: 0;
            padding: 0;
            font-family: 'Poppins', sans-serif;
            background: var(--gradient-bg);
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            overflow-x: hidden;
            position: relative;
        }

        
        .heart-bg {
            position: absolute;
            font-size: 20px;
            color: rgba(26, 115, 232, 0.15);
            animation: floatBackground 8s linear infinite;
            z-index: 0;
            pointer-events: none;
        }

        
        .tap-particle {
            position: absolute;
            pointer-events: none;
            font-size: 24px;
            z-index: 9999;
            animation: burstParticle 0.8s ease-out forwards;
        }

        
        .login-container {
            background-color: var(--container-bg);
            padding: 40px 20px;
            border-radius: 24px;
            box-shadow: var(--shadow-md);
            border: 4px solid #fff;
            text-align: center;
            max-width: 400px;
            width: 85%;
            display: block;
            z-index: 1;
            backdrop-filter: blur(10px);
            transform: translateY(0);
            animation: floatCard 4s ease-in-out infinite;
            box-sizing: border-box;
        }

        h1 {
            font-family: 'Great Vibes', cursive;
            color: var(--accent-color);
            font-size: clamp(2.5rem, 8vw, 3.5rem);
            margin-bottom: 25px;
            text-shadow: 1px 1px 2px rgba(0,0,0,0.05);
        }

        .input-group {
            margin-bottom: 25px;
            position: relative;
        }

        input[type="password"] {
            width: 80%;
            max-width: 280px;
            padding: 14px 20px;
            border: 2px solid rgba(26, 115, 232, 0.3);
            border-radius: 30px;
            font-size: 1.1rem;
            text-align: center;
            outline: none;
            color: var(--text-color);
            transition: all 0.3s ease;
            box-shadow: inset 0 2px 4px rgba(0,0,0,0.02);
        }

        input[type="password"]:focus {
            border-color: var(--accent-color);
            box-shadow: 0 0 10px rgba(26, 115, 232, 0.2);
            width: 85%;
        }

        button {
            background-color: var(--accent-color);
            color: white;
            border: none;
            padding: 14px 45px;
            font-size: 1.1rem;
            border-radius: 30px;
            cursor: pointer;
            font-weight: 600;
            letter-spacing: 0.5px;
            transition: all 0.3s cubic-bezier(0.175, 0.885, 0.32, 1.275);
            box-shadow: 0 4px 15px rgba(26, 115, 232, 0.3);
        }

        button:hover {
            background-color: var(--accent-hover);
            transform: translateY(-3px) scale(1.05);
            box-shadow: 0 6px 20px rgba(26, 115, 232, 0.4);
        }

        button:active {
            transform: translateY(-1px) scale(1.01);
        }

        .shake {
            animation: shake 0.5s ease-in-out;
        }

        /* Welcome Message Screen */
        .welcome-screen {
            display: none;
            font-family: 'Great Vibes', cursive;
            color: var(--accent-color);
            font-size: clamp(3rem, 10vw, 4.5rem);
            text-align: center;
            padding: 20px;
            z-index: 1;
            text-shadow: 2px 2px 4px rgba(255,255,255,0.5);
            animation: welcomeEffect 2.5s ease-in-out forwards;
            width: 90%;
            box-sizing: border-box;
        }

        
        .main-content {
            display: none;
            width: 90%;
            max-width: 800px;
            background-color: var(--container-bg);
            border-radius: 24px;
            border: 4px solid #fff;
            box-shadow: var(--shadow-md);
            padding: clamp(15px, 5vw, 30px);
            margin: 20px auto;
            z-index: 1;
            backdrop-filter: blur(10px);
            animation: slideUpFade 0.8s cubic-bezier(0.16, 1, 0.3, 1) forwards;
            box-sizing: border-box;
        }

        
        .tabs {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: 10px;
            margin-bottom: 30px;
            border-bottom: 2px solid rgba(26, 115, 232, 0.1);
            padding-bottom: 20px;
        }

        .tab-btn {
            background: white;
            color: var(--accent-color);
            border: 2px solid rgba(26, 115, 232, 0.2);
            padding: 12px 10px;
            border-radius: 25px;
            cursor: pointer;
            font-weight: 600;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 6px;
            font-size: clamp(0.85rem, 3vw, 0.95rem);
            box-shadow: var(--shadow-sm);
            transition: all 0.3s ease;
        }

        .tab-btn:hover {
            border-color: var(--accent-color);
            background: rgba(240, 248, 255, 0.5);
            transform: translateY(-2px);
        }

        .tab-btn.active {
            background-color: var(--accent-color);
            color: white;
            border-color: var(--accent-color);
            box-shadow: 0 4px 12px rgba(26, 115, 232, 0.3);
        }

        /* Tab Content areas */
        .tab-content {
            display: none;
            color: var(--text-color);
            transform: translateY(15px);
            opacity: 0;
            transition: all 0.5s ease;
        }

        .tab-content.active {
            display: block;
            animation: tabReveal 0.5s cubic-bezier(0.16, 1, 0.3, 1) forwards;
        }

        
        .letter-box {
            background: white;
            padding: clamp(20px, 5vw, 35px);
            border-radius: 20px;
            border-left: 6px solid var(--accent-color);
            line-height: 1.8;
            font-size: clamp(0.95rem, 3.5vw, 1.05rem);
            box-shadow: var(--shadow-sm);
            position: relative;
            overflow: hidden;
            word-wrap: break-word;
        }

        .letter-box::before {
            content: '💙';
            position: absolute;
            bottom: -20px;
            right: 10px;
            font-size: clamp(5rem, 15vw, 8rem);
            color: rgba(26, 115, 232, 0.03);
            font-family: serif;
        }

        .letter-title {
            font-family: 'Great Vibes', cursive;
            font-size: clamp(2.2rem, 7vw, 3rem);
            color: var(--accent-color);
            margin-top: 0;
            margin-bottom: 15px;
        }

        
        .music-list, .notes-list {
            list-style: none;
            padding: 0;
            margin: 0;
        }

        .music-item {
            background: white;
            padding: 15px 20px;
            border-radius: 15px;
            margin-bottom: 15px;
            box-shadow: var(--shadow-sm);
            display: flex;
            flex-direction: column;
            gap: 10px;
            transition: all 0.3s ease;
            border: 1px solid rgba(26, 115, 232, 0.05);
        }

        .music-item:hover {
            transform: translateY(-2px);
            border-color: rgba(26, 115, 232, 0.2);
        }

        .music-info {
            display: flex;
            align-items: center;
            gap: 10px;
            font-size: clamp(0.9rem, 3.5vw, 1rem);
        }

        .music-item span {
            font-weight: 600;
            color: var(--accent-color);
        }

        .audio-player-mock {
            display: flex;
            align-items: center;
            gap: 12px;
            background: #f0f4f9;
            padding: 8px 15px;
            border-radius: 20px;
        }

        .play-btn {
            background: var(--accent-color);
            width: 32px;
            height: 32px;
            border-radius: 50%;
            padding: 0;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 0.8rem;
            box-shadow: none;
        }

        .play-btn:hover {
            transform: scale(1.1);
            box-shadow: none;
        }

        .progress-bar-mock {
            flex-grow: 1;
            height: 6px;
            background: #ddd;
            border-radius: 3px;
            position: relative;
            overflow: hidden;
        }

        .progress-fill {
            width: 0%;
            height: 100%;
            background: var(--accent-color);
            transition: width 0.1s linear;
        }

        
        .note-item {
            background: white;
            padding: clamp(14px, 4vw, 18px) clamp(18px, 5vw, 25px);
            border-radius: 15px;
            margin-bottom: 15px;
            box-shadow: var(--shadow-sm);
            display: flex;
            align-items: center;
            gap: 12px;
            transition: all 0.3s ease;
            border: 1px solid rgba(26, 115, 232, 0.05);
            font-size: clamp(0.9rem, 3.5vw, 1rem);
            word-break: break-word;
            cursor: pointer;
        }

        .note-item:hover {
            transform: translateX(8px) scale(1.01);
            background: #faffff;
        }

        .heart-wrapper {
            display: flex;
            justify-content: center;
            gap: 15px;
            flex-wrap: wrap;
            margin: 25px 0;
        }

        .candy-heart {
            background: #d0e1fd;
            width: 80px;
            height: 80px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            text-align: center;
            font-size: 0.75rem;
            font-weight: 600;
            color: #1254a8;
            cursor: pointer;
            box-shadow: 0 4px 6px rgba(0,0,0,0.05);
            transition: transform 0.2s, background-color 0.3s;
            user-select: none;
            padding: 5px;
            box-sizing: border-box;
        }

        .candy-heart:hover {
            transform: scale(1.1) rotate(5deg);
            background: #badefc;
        }

        
        .coupon-card {
            background: white;
            border: 2px dashed var(--accent-color);
            border-radius: 15px;
            padding: 20px;
            margin: 20px 0;
            text-align: center;
            position: relative;
            box-shadow: var(--shadow-sm);
        }

        .coupon-title {
            font-family: 'Great Vibes', cursive;
            font-size: 2rem;
            color: var(--accent-color);
            margin: 0 0 10px 0;
        }

        .coupon-btn {
            background-color: var(--accent-color);
            color: white;
            border: none;
            padding: 8px 20px;
            font-size: 0.9rem;
            border-radius: 20px;
            cursor: pointer;
            font-weight: 600;
            margin-top: 10px;
            transition: all 0.3s ease;
        }

        .coupon-btn:disabled {
            background-color: #ccc;
            cursor: not-allowed;
            transform: none;
            box-shadow: none;
        }

        
        .gallery-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(min(100%, 220px), 1fr));
            gap: 15px;
        }

        .gallery-item {
            overflow: hidden;
            border-radius: 15px;
            border: 4px solid white;
            box-shadow: 0 6px 16px rgba(0,0,0,0.08);
            transition: all 0.4s cubic-bezier(0.165, 0.84, 0.44, 1);
            position: relative;
            height: 200px;
            cursor: pointer;
        }

        .gallery-grid img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            transition: transform 0.5s ease;
        }

        .gallery-item:hover {
            transform: translateY(-5px) rotate(2deg);
            box-shadow: 0 12px 25px rgba(26, 115, 232, 0.2);
            border-color: #f0f8ff;
        }

        .gallery-item:hover img {
            transform: scale(1.1);
        }

        .photo-caption {
            position: absolute;
            bottom: 0;
            left: 0;
            right: 0;
            background: linear-gradient(transparent, rgba(0,0,0,0.7));
            color: white;
            padding: 25px 10px 10px 10px;
            font-size: 0.85rem;
            text-align: center;
            opacity: 0;
            transition: opacity 0.3s ease;
        }

        .gallery-item:hover .photo-caption {
            opacity: 1;
        }

        h3 {
            font-size: clamp(1.2rem, 5vw, 1.5rem);
        }

        
        @media (min-width: 600px) {
            .tabs {
                grid-template-columns: repeat(4, 1fr);
            }
            .gallery-item {
                height: 220px;
            }
        }

        
        @keyframes floatCard {
            0%, 100% { transform: translateY(0); }
            50% { transform: translateY(-10px); }
        }

        @keyframes floatBackground {
            0% { transform: translateY(100vh) translateX(0) rotate(0deg); opacity: 0; }
            10% { opacity: 1; }
            90% { opacity: 1; }
            100% { transform: translateY(-10vh) translateX(50px) rotate(360deg); opacity: 0; }
        }

        @keyframes welcomeEffect {
            0% { opacity: 0; transform: scale(0.8); filter: blur(5px); }
            30% { opacity: 1; transform: scale(1.05); filter: blur(0); }
            70% { opacity: 1; transform: scale(1); filter: blur(0); }
            100% { opacity: 0; transform: scale(1.1); filter: blur(3px); }
        }

        @keyframes slideUpFade {
            from { opacity: 0; transform: translateY(40px); }
            to { opacity: 1; transform: translateY(0); }
        }

        @keyframes tabReveal {
            from { opacity: 0; transform: translateY(15px); }
            to { opacity: 1; transform: translateY(0); }
        }

        @keyframes shake {
            0%, 100% { transform: translateX(0); }
            20%, 60% { transform: translateX(-10px); }
            40%, 80% { transform: translateX(10px); }
        }

        @keyframes burstParticle {
            0% { transform: translate(0, 0) scale(1); opacity: 1; }
            100% { transform: translate(var(--mx), var(--my)) scale(0.3); opacity: 0; }
        }
    </style>
</head>
<body>

    <script>
        const heartCount = window.innerWidth < 600 ? 10 : 20;
        for (let i = 0; i < heartCount; i++) {
            const heart = document.createElement('div');
            heart.className = 'heart-bg';
            heart.innerText = ['💙', '💎', '✨', '🌊'][Math.floor(Math.random() * 4)];
            heart.style.left = Math.random() * 100 + 'vw';
            heart.style.animationDelay = Math.random() * 8 + 's';
            heart.style.fontSize = Math.random() * 15 + 15 + 'px';
            document.body.appendChild(heart);
        }

        
        document.addEventListener('click', (e) => {
            if(e.target.tagName === 'BUTTON' || e.target.closest('.gallery-item') || e.target.closest('.note-item') || e.target.closest('.candy-heart')) {
                const burstItems = ['💙', '✨', '💎', '🌸', '🎈'];
                for(let i=0; i<6; i++) {
                    const p = document.createElement('div');
                    p.className = 'tap-particle';
                    p.innerText = burstItems[Math.floor(Math.random() * burstItems.length)];
                    p.style.left = e.pageX + 'px';
                    p.style.top = e.pageY + 'px';
                    
                    const angle = Math.random() * Math.PI * 2;
                    const velocity = Math.random() * 60 + 40;
                    p.style.setProperty('--mx', Math.cos(angle) * velocity + 'px');
                    p.style.setProperty('--my', Math.sin(angle) * velocity + 'px');
                    
                    document.body.appendChild(p);
                    setTimeout(() => p.remove(), 800);
                }
            }
        });
    </script>

    <div class="login-container" id="loginScreen">
        <h1>Enter the secret code</h1>
        <div class="input-group">
            <input type="password" id="passwordInput" placeholder="••••••••">
        </div>
        <button onclick="checkPassword()">Enter</button>
    </div>

    <div class="welcome-screen" id="welcomeScreen">
        Welcome my Alexis
    </div>

    <div class="main-content" id="mainDashboard">
        <div class="tabs">
            <button class="tab-btn active" onclick="switchTab(event, 'loveLetter')">💙 Letter</button>
            <button class="tab-btn" onclick="switchTab(event, 'music')">🎵 Music</button>
            <button class="tab-btn" onclick="switchTab(event, 'notes')">📝 Notes</button>
            <button class="tab-btn" onclick="switchTab(event, 'gallery')">🖼️ Gallery</button>
        </div>

        <div id="loveLetter" class="tab-content active">
            <div class="letter-box">
                <h2 class="letter-title">My Dearest</h2>
                <p>My love,<br>Happy first monthsary, my love! I can’t believe it has already been a whole month since we made things official. Looking back at the past thirty days not the thirty days pala I mean the days nga always hanging out every after class, my heart is so full of gratitude for every laugh, every text, and every quiet moment we’ve shared. You have quickly become my favorite part of every day, and even the simplest moments feel incredibly special when I'm with you.</p>
                <p>Thank you for bringing so much warmth, happiness, and peace into my life. The way you care for me, listen to me, and make me smile effortlessly reminds me every day of how lucky I am to have you. You have a beautiful way of making the world feel brighter, and being loved by you is a gift I will never take for granted.</p>
                <p>This month is just the beginning of our beautiful journey together, and I am so excited for everything the future holds for us. I promise to keep choosing you, supporting you, and loving you more with each passing day. Happy monthsary, sweetheart here is to us and to many more beautiful months ahead.</p>
                <p>Forever yours, <br>💙</p>
            </div>
        </div>

        <div id="music" class="tab-content">
            <h3 style="color: var(--accent-color); margin-bottom: 20px;">Songs that remind me of you</h3>
            <ul class="music-list">
                <li class="music-item">
                    <div class="music-info">🎵 <span>First Love</span> &nbsp;by Ardhito Pramono</div>
                    <div class="audio-player-mock">
                        <button class="play-btn" onclick="togglePlayMock(this)">▶</button>
                        <div class="progress-bar-mock"><div class="progress-fill"></div></div>
                    </div>
                </li>
                <li class="music-item">
                    <div class="music-info">🎵 <span>Kalapastangan</span> &nbsp;by Fitterkarma</div>
                    <div class="audio-player-mock">
                        <button class="play-btn" onclick="togglePlayMock(this)">▶</button>
                        <div class="progress-bar-mock"><div class="progress-fill"></div></div>
                    </div>
                </li>
            </ul>
        </div>

        <div id="notes" class="tab-content">
            <h3 style="color: var(--accent-color); margin-bottom: 10px;">Some reminders</h3>
            <p style="font-size:0.85rem; color:#777; margin:0 0 15px 0;">✨ Tap any note for a little extra dynamic touch!</p>
            <ul class="notes-list">
                <li class="note-item" onclick="popNote(this, 'Take care always, because you carry my whole world with you 🌍')">✨ Take care always</li>
                <li class="note-item" onclick="popNote(this, 'No skipping meals! Fast food or homecooked, fill up that stomach 🍲')">✨ Eat on time</li>
                <li class="note-item" onclick="popNote(this, 'Locked and sealed 🔐 permanently claimed property right here!')">✨ You're mine, always</li>
                <li class="note-item" onclick="popNote(this, 'Your grades do not define my love for you. Rest your brilliant mind 🛌')">✨ Don't study too hard, rest when you need to</li>
                <li class="note-item" onclick="popNote(this, 'Cheering for you loud and clear through every success and every bump 📣')">✨ I am always proud of you, no matter what</li>
            </ul>

            <h3 style="color: var(--accent-color); margin: 25px 0 10px 0;">Conversation Candy Hearts</h3>
            <div class="heart-wrapper">
                <div class="candy-heart" onclick="tapCandyHeart(this, 'I LOVE U')">TAP<br>ME</div>
                <div class="candy-heart" onclick="tapCandyHeart(this, 'BE MINE')">TAP<br>ME</div>
                <div class="candy-heart" onclick="tapCandyHeart(this, 'CUTIE PIE')">TAP<br>ME</div>
                <div class="candy-heart" onclick="tapCandyHeart(this, 'MY ALL')">TAP<br>ME</div>
            </div>

            <div class="coupon-card">
                <h4 class="coupon-title">Special Love Coupon</h4>
                <p style="margin: 5px 0 15px 0; font-size: 0.95rem;">Redeemable for one dynamic study date breakaway + heavy snacks on me ☕🍰</p>
                <button class="coupon-btn" id="couponBtn" onclick="claimCoupon()">Claim Coupon</button>
            </div>
        </div>

        <div id="gallery" class="tab-content">
            <h3 style="color: var(--accent-color); margin-bottom: 20px;">Our Moments</h3>
            <div class="gallery-grid">
                <div class="gallery-item">
                    <img src="https://i.ibb.co/SDHF6tb1/f1dfc14f-c9e0-4f23-9d9c-67ec9f6186d4.jpg" alt="">
                    <div class="photo-caption">My prettiest view 🌊</div>
                </div>
                <div class="gallery-item">
                    <img src="https://i.ibb.co/tpPQnjCw/5cb0de70-5fb3-430f-9a41-f1c29239aee3.jpg" alt="">
                    <div class="photo-caption">Hanging out after class ☕</div>
                </div>
                <div class="gallery-item">
                    <img src="https://i.ibb.co/tpyzMM08/1892cefc-9f66-4e27-8f96-0750354bb20b.jpg" alt="">
                    <div class="photo-caption">Treasured moments 💙</div>
                </div>
                <div class="gallery-item">
                    <img src="https://i.ibb.co/LX5sFNTx/255e5571-f837-4d0f-8bf6-f762b8a1134e.jpg" alt="">
                    <div class="photo-caption">Every second with you is precious ✨</div>
                </div>
                <div class="gallery-item">
                    <img src="https://i.ibb.co/0VRDm1CS/894e762a-d41a-4058-9242-f022f4b0f738.jpg" alt="">
                    <div class="photo-caption">Smiles generated effortlessly 🥰</div>
                </div>
                <div class="gallery-item">
                    <img src="https://i.ibb.co/nNZj7Fqj/1261e691-836d-4ccf-b1ce-0c20c326f215.jpg" alt="">
                    <div class="photo-caption">To infinity and beyond 💎</div>
                </div>
            </div>
        </div>
    </div>

    <script>
        function checkPassword() {
            const password = document.getElementById('passwordInput').value;
            const loginContainer = document.getElementById('loginScreen');
            
            if (password.toLowerCase() === '051626') {
                loginContainer.style.display = 'none';
                const welcome = document.getElementById('welcomeScreen');
                welcome.style.display = 'block';

                setTimeout(() => {
                    welcome.style.display = 'none';
                    document.getElementById('mainDashboard').style.display = 'block';
                }, 2400); 
            } else {
                loginContainer.classList.add('shake');
                setTimeout(() => loginContainer.classList.remove('shake'), 500);
                alert('Incorrect secret code! Try again 💙');
            }
        }

        function switchTab(event, tabId) {
            const tabContents = document.getElementsByClassName('tab-content');
            for (let i = 0; i < tabContents.length; i++) {
                tabContents[i].classList.remove('active');
            }

            const tabButtons = document.getElementsByClassName('tab-btn');
            for (let i = 0; i < tabButtons.length; i++) {
                tabButtons[i].classList.remove('active');
            }

            document.getElementById(tabId).classList.add('active');
            event.currentTarget.classList.add('active');
        }

        function claimCoupon() {
            const btn = document.getElementById('couponBtn');
            btn.innerText = 'Claimed! Screenshot this 💙';
            btn.disabled = true;
            btn.style.backgroundColor = '#28a745';
        }

        function popNote(element, message) {
            element.style.transform = "scale(0.95)";
            setTimeout(() => {
                element.style.transform = "none";
                alert(message);
            }, 100);
        }

        function tapCandyHeart(heart, message) {
            heart.innerText = message;
            heart.style.backgroundColor = '#fbc2eb';
            heart.style.color = '#aa1459';
            setTimeout(() => {
                heart.innerText = 'TAP\nME';
                heart.style.backgroundColor = '#d0e1fd';
                heart.style.color = '#1254a8';
            }, 2000);
        }

        let activeIntervals = new Map();
        function togglePlayMock(btn) {
            const fill = btn.nextElementSibling.querySelector('.progress-fill');
            if (btn.innerText === '▶') {
                btn.innerText = '⏸';
                let progress = parseFloat(fill.style.width) || 0;
                const interval = setInterval(() => {
                    progress += 1;
                    if (progress > 100) progress = 0;
                    fill.style.width = progress + '%';
                }, 200);
                activeIntervals.set(btn, interval);
            } else {
                btn.innerText = '▶';
                clearInterval(activeIntervals.get(btn));
                activeIntervals.delete(btn);
            }
        }
    </script>
</body>
</html>
